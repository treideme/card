[constants]
arch = 'msp430-elf-'

[binaries]
ar = arch + 'ar'
as = arch + 'as'
c = arch + 'gcc'
cpp = arch + 'g++'
gdb = arch + 'gdb'
ld = arch + 'ld'
size = arch + 'size'
strip = arch + 'strip'

[built-in options]
c_std = 'gnu99'
default_library = 'static'
werror = true
c_args = [
    '-fshort-enums',
    '-ffunction-sections',
    '-Wall',
    '-Werror',
    '-Wextra',
    '-pedantic',
    '-gdwarf-3',
    '-gstrict-dwarf', 
    '-msmall',
    '-mhwmult=none',
    ]

c_link_args = [
    '-Wall',
    '-Werror',
    '-gdwarf-3',
    '-gstrict-dwarf',
    '-msmall',
    '-Wl,--gc-sections',
    '-Wl,--start-group',
    '-lgcc',
    '-lc',
    '-Wl,--end-group',
    ]

[host_machine]
system = 'bare'
cpu_family = 'msp430'
cpu = 'msp430'
endian = 'none'
